{
  "name": "functions",
  "version": "1.0.0",
  "description": "Sully Academy Firebase backend functions",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions:booking",
    "deploy": "npm run build && firebase deploy --only functions:booking --project booking"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.0",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1",
    "typescript": "^7.0.2"
  }
}
